//'use strict';
//pi = 3.1416; // no se muestra en consola, es necesario declarar antes de asignar.
//console.log(pi);

function myFunction() {
   // 'use strict';
    return pi = 3.1416; // nuevamente, es necesario declarar antes de asignar.
}

console.log(myFunction());